{
  "name": "@fusebox/api",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --env-file=../../.env --hot --inspect=localhost:6499/debug src/index.ts",
    "start": "NODE_ENV=production bun run --env-file=../../.env src/index.ts"
  },
  "dependencies": {
    "@fusebox/tsconfig": "workspace:^",
    "@hono/zod-validator": "^0.7.2",
    "hono": "^4.9.0",
    "hono-pino": "^0.10.1",
    "pino": "^9.8.0",
    "pino-pretty": "^13.1.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
